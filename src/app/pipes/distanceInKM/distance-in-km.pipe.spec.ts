import { DistanceInKMPipe } from './distance-in-km.pipe';

describe('DistanceInKMPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceInKMPipe();
    expect(pipe).toBeTruthy();
  });
});
